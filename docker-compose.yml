services:
  # ============================================
  # PostgreSQL Database with Extensions
  # ============================================
  postgres:
    build:
      context: .
      dockerfile: deploy/deployment/Dockerfile.postgres
      cache_from:
        - rag_postgres:latest
    image: rag_postgres:latest
    container_name: rag_postgres
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 512M
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-rag_db}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=en_US.UTF-8"
    ports:
      - "5432:5432"
    volumes:
      # Persistent data storage
      - postgres_data:/var/lib/postgresql/data
      # Initialization scripts (migrations)
      - ./deploy/migrations:/docker-entrypoint-initdb.d:ro
    networks:
      - rag_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ============================================
  # Redis (Celery Broker/Backend)
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: rag_redis
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M
    ports:
      - "6379:6379"
    networks:
      - rag_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ============================================
  # RAG Application
  # ============================================
  app:
    build:
      context: .
      dockerfile: deploy/deployment/Dockerfile
      cache_from:
        - rag_with_llama:latest
    image: rag_with_llama:latest
    container_name: rag_app
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    environment:
      # Database connection
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-rag_db}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_URL: postgresql://${POSTGRES_USER:?}:${POSTGRES_PASSWORD:?}@postgres:5432/${POSTGRES_DB:-rag_db}

      # Ollama (runs on Windows host, accessible via host.docker.internal)
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-deepseek-r1:1.5b}
      OLLAMA_VLM_MODEL: ${OLLAMA_VLM_MODEL:-qwen3.5:0.8b}

      # API Keys
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      LOGFIRE_WRITE_TOKEN: ${LOGFIRE_WRITE_TOKEN:-}
      LOGFIRE_READ_TOKEN: ${LOGFIRE_READ_TOKEN:-}

      # LLM
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}

      # Langfuse observability (optional — leave blank to disable)
      LANGFUSE_HOST: ${LANGFUSE_HOST:-http://langfuse:3000}
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}

      # Logging
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      # Celery broker/backend + optional FE password
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      APP_ACCESS_PASSWORD: ${APP_ACCESS_PASSWORD:-}

      # Ingestion settings
      INPUT_RAW_DIR: ${INPUT_RAW_DIR:-input/raw}
      PARSED_DIR: ${PARSED_DIR:-data/parsed}
      CHUNKS_DIR: ${CHUNKS_DIR:-data/chunks}
      PERSIST_INGESTION_ARTIFACTS: ${PERSIST_INGESTION_ARTIFACTS:-true}
      INGESTION_MAX_ATTEMPTS: ${INGESTION_MAX_ATTEMPTS:-2}
      # 180, not 30: a 500-page parse runs ~60 minutes, and a shorter timeout
      # lets the 6-hourly recovery sweep declare a live parse stale, re-dispatch a
      # second conversion of the same document, and burn the retry budget.
      INGESTION_CLAIM_TIMEOUT_MINUTES: ${INGESTION_CLAIM_TIMEOUT_MINUTES:-180}
      DEFAULT_CHUNK_SIZE: ${DEFAULT_CHUNK_SIZE:-512}
      DEFAULT_PARSE_BACKEND: ${DEFAULT_PARSE_BACKEND:-ollama}

      # PDF parse tuning. These defaults reproduce the previously hardcoded
      # values exactly; they are here so the CPU/thread and VLM-concurrency
      # experiments are env changes rather than code edits.
      # 4, targeting the M1's 4 performance cores, with slack left inside the
      # 4.0 CPU quota for the orchestrating thread and asyncpg — Docker enforces
      # --cpus as a CFS bandwidth quota, so saturating it throttles everything
      # else in the container rather than just failing to help.
      DOCLING_NUM_THREADS: ${DOCLING_NUM_THREADS:-4}
      DOCLING_PAGE_BATCH_SIZE: ${DOCLING_PAGE_BATCH_SIZE:-50}
      DOCLING_TABLEFORMER_MODE: ${DOCLING_TABLEFORMER_MODE:-fast}
      # 1, not 2: Ollama runs on the same host and serializes on one GPU.
      # Measured on an M1 — 3.87s/call at 1, 4.93s at 2, 20.62s at 4.
      VLM_CONCURRENCY: ${VLM_CONCURRENCY:-1}
      # qwen3.5 is a reasoning model and Ollama defaults thinking ON. Measured
      # on a real BERT table crop: 87.35s versus 2.27s, with the reasoning
      # discarded unread. Tables additionally returned empty every time.
      OLLAMA_VLM_THINK: ${OLLAMA_VLM_THINK:-false}
      # Tables go to docling's TableFormer. A 0.8B VLM cannot read them: on
      # bert.pdf the 13-column GLUE table came back with headers "I, II, III…"
      # and a small table came back with invented rows. Set true to restore the
      # old behaviour of routing complex tables to the VLM.
      VLM_TABLES: ${VLM_TABLES:-false}
      # Latency here is pure decode at ~35 tok/s, so elapsed IS the output
      # length — and Ollama's defaults (temperature 0.8, num_predict -1)
      # leave it unbounded. Measured: the same 218x54px crop returned 22
      # tokens in 1.55s and 342 tokens of invented flowchart in 10.94s on
      # consecutive identical requests, and the worst call of a 191-call
      # run took 93.3s. Greedy decoding gives 22-35 tokens and transcribes
      # the crop instead of inventing one.
      OLLAMA_VLM_TEMPERATURE: ${OLLAMA_VLM_TEMPERATURE:-0.0}
      OLLAMA_VLM_NUM_PREDICT: ${OLLAMA_VLM_NUM_PREDICT:-384}
      # Short-side floor for sending a picture to the VLM. 113 of those 191
      # calls were strips under 64px tall — rules and equation lines, not
      # figures — and they consumed 60% of the VLM budget hallucinating
      # content that then gets embedded.
      VLM_IMAGES_SCALE: ${VLM_IMAGES_SCALE:-2.0}
      VLM_MIN_IMAGE_SHORT_PT: ${VLM_MIN_IMAGE_SHORT_PT:-107}

      # The runtime image ships without a compiler by design, so a downstream
      # torch.compile path (torchvision NMS during docling's layout pass) dies
      # with InvalidCxxCompiler. Nothing here calls torch.compile, so forcing
      # eager execution costs nothing and removes the crash.
      TORCHDYNAMO_DISABLE: "1"
    ports:
      - "8000:8000"
    volumes:
      # Mount code for development (hot reload)
      - .:/app
      # Logs
      - ./logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - rag_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  # ============================================
  # Celery Worker - Upload queue (single worker)
  # Serves interactive uploads from the UI, kept separate from the
  # ingestion queue so a long directory scan cannot starve them.
  # ============================================
  celery_worker_upload:
    build:
      context: .
      dockerfile: deploy/deployment/Dockerfile
      cache_from:
        - rag_with_llama:latest
    image: rag_with_llama:latest
    container_name: rag_celery_worker_upload
    deploy:
      resources:
        limits:
          cpus: "4.0"
          # 3.5G, not 2.5G. A 504-page parse was OOM-killed (signal 9,
          # OOMKilled=true) on its final batch at the old ceiling: RSS climbed
          # 1589MB -> 1850MB across nine batches and the run before it peaked at
          # 1979MB, i.e. 79% of 2.5G. Applying the Docker Desktop CPU slider
          # (docker info NCPU 4 -> 6) roughly doubled docling throughput to
          # ~1.5s/page and the extra in-flight work consumed what headroom was
          # left. Only this worker is raised — celery_worker_ingestion stays at
          # 2.5G, because the limits already sum to more than the 7.27G VM and
          # two simultaneous parses are the case that would exhaust it.
          memory: 3.5G
        reservations:
          cpus: "0.5"
          memory: 512M
    command: celery -A app.worker.celery_app worker -Q upload --loglevel=info -c 1 --max-tasks-per-child=1
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-rag_db}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_URL: postgresql://${POSTGRES_USER:?}:${POSTGRES_PASSWORD:?}@postgres:5432/${POSTGRES_DB:-rag_db}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-deepseek-r1:1.5b}
      OLLAMA_VLM_MODEL: ${OLLAMA_VLM_MODEL:-qwen3.5:0.8b}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      INPUT_RAW_DIR: ${INPUT_RAW_DIR:-input/raw}
      PARSED_DIR: ${PARSED_DIR:-data/parsed}
      CHUNKS_DIR: ${CHUNKS_DIR:-data/chunks}
      PERSIST_INGESTION_ARTIFACTS: ${PERSIST_INGESTION_ARTIFACTS:-true}
      INGESTION_MAX_ATTEMPTS: ${INGESTION_MAX_ATTEMPTS:-2}
      # 180, not 30: a 500-page parse runs ~60 minutes, and a shorter timeout
      # lets the 6-hourly recovery sweep declare a live parse stale, re-dispatch a
      # second conversion of the same document, and burn the retry budget.
      INGESTION_CLAIM_TIMEOUT_MINUTES: ${INGESTION_CLAIM_TIMEOUT_MINUTES:-180}
      DEFAULT_CHUNK_SIZE: ${DEFAULT_CHUNK_SIZE:-512}
      DEFAULT_PARSE_BACKEND: ${DEFAULT_PARSE_BACKEND:-ollama}

      # PDF parse tuning. These defaults reproduce the previously hardcoded
      # values exactly; they are here so the CPU/thread and VLM-concurrency
      # experiments are env changes rather than code edits.
      # 4, targeting the M1's 4 performance cores, with slack left inside the
      # 4.0 CPU quota for the orchestrating thread and asyncpg — Docker enforces
      # --cpus as a CFS bandwidth quota, so saturating it throttles everything
      # else in the container rather than just failing to help.
      DOCLING_NUM_THREADS: ${DOCLING_NUM_THREADS:-4}
      DOCLING_PAGE_BATCH_SIZE: ${DOCLING_PAGE_BATCH_SIZE:-50}
      DOCLING_TABLEFORMER_MODE: ${DOCLING_TABLEFORMER_MODE:-fast}
      # 1, not 2: Ollama runs on the same host and serializes on one GPU.
      # Measured on an M1 — 3.87s/call at 1, 4.93s at 2, 20.62s at 4.
      VLM_CONCURRENCY: ${VLM_CONCURRENCY:-1}
      # qwen3.5 is a reasoning model and Ollama defaults thinking ON. Measured
      # on a real BERT table crop: 87.35s versus 2.27s, with the reasoning
      # discarded unread. Tables additionally returned empty every time.
      OLLAMA_VLM_THINK: ${OLLAMA_VLM_THINK:-false}
      # Tables go to docling's TableFormer. A 0.8B VLM cannot read them: on
      # bert.pdf the 13-column GLUE table came back with headers "I, II, III…"
      # and a small table came back with invented rows. Set true to restore the
      # old behaviour of routing complex tables to the VLM.
      VLM_TABLES: ${VLM_TABLES:-false}
      # Latency here is pure decode at ~35 tok/s, so elapsed IS the output
      # length — and Ollama's defaults (temperature 0.8, num_predict -1)
      # leave it unbounded. Measured: the same 218x54px crop returned 22
      # tokens in 1.55s and 342 tokens of invented flowchart in 10.94s on
      # consecutive identical requests, and the worst call of a 191-call
      # run took 93.3s. Greedy decoding gives 22-35 tokens and transcribes
      # the crop instead of inventing one.
      OLLAMA_VLM_TEMPERATURE: ${OLLAMA_VLM_TEMPERATURE:-0.0}
      OLLAMA_VLM_NUM_PREDICT: ${OLLAMA_VLM_NUM_PREDICT:-384}
      # Short-side floor for sending a picture to the VLM. 113 of those 191
      # calls were strips under 64px tall — rules and equation lines, not
      # figures — and they consumed 60% of the VLM budget hallucinating
      # content that then gets embedded.
      VLM_IMAGES_SCALE: ${VLM_IMAGES_SCALE:-2.0}
      VLM_MIN_IMAGE_SHORT_PT: ${VLM_MIN_IMAGE_SHORT_PT:-107}

      # The runtime image ships without a compiler by design, so a downstream
      # torch.compile path (torchvision NMS during docling's layout pass) dies
      # with InvalidCxxCompiler. Nothing here calls torch.compile, so forcing
      # eager execution costs nothing and removes the crash.
      TORCHDYNAMO_DISABLE: "1"

      # huggingface_hub >=0.32 routes supported repos through the Xet backend
      # (cas-server.xethub.hf.co). Measured here: the .incomplete file stays at
      # exactly 0 bytes while the transfer stalls, because Xet buffers chunks in
      # its own store and only materialises the file at the end — so nothing
      # resumes and a failed download restarts from scratch. Two parses hung
      # 12+ minutes in docling's pipeline init before reaching page 1. Classic
      # HTTP range requests write into .incomplete incrementally and do resume.
      HF_HUB_DISABLE_XET: "1"
      # Defaults are 10s each, and the HEAD revalidation against huggingface.co
      # timed out repeatedly: "Read timed out. (read timeout=10)" on
      # docling-layout-heron/model.safetensors.
      HF_HUB_ETAG_TIMEOUT: "30"
      HF_HUB_DOWNLOAD_TIMEOUT: "60"
    volumes:
      - .:/app
      - ./logs:/app/logs
      # docling's model weights. Without this they live in the container's
      # writable layer and every recreate re-downloads ~385MB before page 1.
      - hf_cache:/home/appuser/.cache/huggingface
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - rag_network
    restart: unless-stopped

  # ============================================
  # Celery Worker - Ingestion queue (scale to 2)
  # Scale with: docker compose up --scale celery_worker_ingestion=2
  # ============================================
  celery_worker_ingestion:
    build:
      context: .
      dockerfile: deploy/deployment/Dockerfile
      cache_from:
        - rag_with_llama:latest
    image: rag_with_llama:latest
    container_name: rag_celery_worker_ingestion
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 2.5G
        reservations:
          cpus: "0.5"
          memory: 512M
    command: celery -A app.worker.celery_app worker -Q ingestion --loglevel=info -c 1 --max-tasks-per-child=1
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-rag_db}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_URL: postgresql://${POSTGRES_USER:?}:${POSTGRES_PASSWORD:?}@postgres:5432/${POSTGRES_DB:-rag_db}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-deepseek-r1:1.5b}
      OLLAMA_VLM_MODEL: ${OLLAMA_VLM_MODEL:-qwen3.5:0.8b}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      INPUT_RAW_DIR: ${INPUT_RAW_DIR:-input/raw}
      PARSED_DIR: ${PARSED_DIR:-data/parsed}
      CHUNKS_DIR: ${CHUNKS_DIR:-data/chunks}
      PERSIST_INGESTION_ARTIFACTS: ${PERSIST_INGESTION_ARTIFACTS:-true}
      INGESTION_MAX_ATTEMPTS: ${INGESTION_MAX_ATTEMPTS:-2}
      # 180, not 30: a 500-page parse runs ~60 minutes, and a shorter timeout
      # lets the 6-hourly recovery sweep declare a live parse stale, re-dispatch a
      # second conversion of the same document, and burn the retry budget.
      INGESTION_CLAIM_TIMEOUT_MINUTES: ${INGESTION_CLAIM_TIMEOUT_MINUTES:-180}
      DEFAULT_CHUNK_SIZE: ${DEFAULT_CHUNK_SIZE:-512}
      DEFAULT_PARSE_BACKEND: ${DEFAULT_PARSE_BACKEND:-ollama}

      # PDF parse tuning. These defaults reproduce the previously hardcoded
      # values exactly; they are here so the CPU/thread and VLM-concurrency
      # experiments are env changes rather than code edits.
      # 4, targeting the M1's 4 performance cores, with slack left inside the
      # 4.0 CPU quota for the orchestrating thread and asyncpg — Docker enforces
      # --cpus as a CFS bandwidth quota, so saturating it throttles everything
      # else in the container rather than just failing to help.
      DOCLING_NUM_THREADS: ${DOCLING_NUM_THREADS:-4}
      DOCLING_PAGE_BATCH_SIZE: ${DOCLING_PAGE_BATCH_SIZE:-50}
      DOCLING_TABLEFORMER_MODE: ${DOCLING_TABLEFORMER_MODE:-fast}
      # 1, not 2: Ollama runs on the same host and serializes on one GPU.
      # Measured on an M1 — 3.87s/call at 1, 4.93s at 2, 20.62s at 4.
      VLM_CONCURRENCY: ${VLM_CONCURRENCY:-1}
      # qwen3.5 is a reasoning model and Ollama defaults thinking ON. Measured
      # on a real BERT table crop: 87.35s versus 2.27s, with the reasoning
      # discarded unread. Tables additionally returned empty every time.
      OLLAMA_VLM_THINK: ${OLLAMA_VLM_THINK:-false}
      # Tables go to docling's TableFormer. A 0.8B VLM cannot read them: on
      # bert.pdf the 13-column GLUE table came back with headers "I, II, III…"
      # and a small table came back with invented rows. Set true to restore the
      # old behaviour of routing complex tables to the VLM.
      VLM_TABLES: ${VLM_TABLES:-false}
      # Latency here is pure decode at ~35 tok/s, so elapsed IS the output
      # length — and Ollama's defaults (temperature 0.8, num_predict -1)
      # leave it unbounded. Measured: the same 218x54px crop returned 22
      # tokens in 1.55s and 342 tokens of invented flowchart in 10.94s on
      # consecutive identical requests, and the worst call of a 191-call
      # run took 93.3s. Greedy decoding gives 22-35 tokens and transcribes
      # the crop instead of inventing one.
      OLLAMA_VLM_TEMPERATURE: ${OLLAMA_VLM_TEMPERATURE:-0.0}
      OLLAMA_VLM_NUM_PREDICT: ${OLLAMA_VLM_NUM_PREDICT:-384}
      # Short-side floor for sending a picture to the VLM. 113 of those 191
      # calls were strips under 64px tall — rules and equation lines, not
      # figures — and they consumed 60% of the VLM budget hallucinating
      # content that then gets embedded.
      VLM_IMAGES_SCALE: ${VLM_IMAGES_SCALE:-2.0}
      VLM_MIN_IMAGE_SHORT_PT: ${VLM_MIN_IMAGE_SHORT_PT:-107}

      # The runtime image ships without a compiler by design, so a downstream
      # torch.compile path (torchvision NMS during docling's layout pass) dies
      # with InvalidCxxCompiler. Nothing here calls torch.compile, so forcing
      # eager execution costs nothing and removes the crash.
      TORCHDYNAMO_DISABLE: "1"

      # See celery_worker_upload above: Xet downloads do not resume, and the
      # 10s default etag timeout was not enough for huggingface.co on this link.
      HF_HUB_DISABLE_XET: "1"
      HF_HUB_ETAG_TIMEOUT: "30"
      HF_HUB_DOWNLOAD_TIMEOUT: "60"
    volumes:
      - .:/app
      - ./logs:/app/logs
      # Shared with celery_worker_upload — both run docling and both would
      # otherwise download the same ~385MB of weights into their own layer.
      - hf_cache:/home/appuser/.cache/huggingface
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - rag_network
    restart: unless-stopped

  # ============================================
  # Celery Beat - Scheduler
  # ============================================
  celery_beat:
    build:
      context: .
      dockerfile: deploy/deployment/Dockerfile
      cache_from:
        - rag_with_llama:latest
    image: rag_with_llama:latest
    container_name: rag_celery_beat
    deploy:
      resources:
        limits:
          memory: 256M
    command: celery -A app.worker.celery_app beat --loglevel=info
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-rag_db}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      DATABASE_URL: postgresql://${POSTGRES_USER:?}:${POSTGRES_PASSWORD:?}@postgres:5432/${POSTGRES_DB:-rag_db}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-deepseek-r1:1.5b}
      OLLAMA_VLM_MODEL: ${OLLAMA_VLM_MODEL:-qwen3.5:0.8b}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      INPUT_RAW_DIR: ${INPUT_RAW_DIR:-input/raw}
      PARSED_DIR: ${PARSED_DIR:-data/parsed}
      CHUNKS_DIR: ${CHUNKS_DIR:-data/chunks}
      PERSIST_INGESTION_ARTIFACTS: ${PERSIST_INGESTION_ARTIFACTS:-true}
      INGESTION_MAX_ATTEMPTS: ${INGESTION_MAX_ATTEMPTS:-2}
      # 180, not 30: a 500-page parse runs ~60 minutes, and a shorter timeout
      # lets the 6-hourly recovery sweep declare a live parse stale, re-dispatch a
      # second conversion of the same document, and burn the retry budget.
      INGESTION_CLAIM_TIMEOUT_MINUTES: ${INGESTION_CLAIM_TIMEOUT_MINUTES:-180}
      DEFAULT_CHUNK_SIZE: ${DEFAULT_CHUNK_SIZE:-512}
      DEFAULT_PARSE_BACKEND: ${DEFAULT_PARSE_BACKEND:-ollama}

      # PDF parse tuning. These defaults reproduce the previously hardcoded
      # values exactly; they are here so the CPU/thread and VLM-concurrency
      # experiments are env changes rather than code edits.
      # 4, targeting the M1's 4 performance cores, with slack left inside the
      # 4.0 CPU quota for the orchestrating thread and asyncpg — Docker enforces
      # --cpus as a CFS bandwidth quota, so saturating it throttles everything
      # else in the container rather than just failing to help.
      DOCLING_NUM_THREADS: ${DOCLING_NUM_THREADS:-4}
      DOCLING_PAGE_BATCH_SIZE: ${DOCLING_PAGE_BATCH_SIZE:-50}
      DOCLING_TABLEFORMER_MODE: ${DOCLING_TABLEFORMER_MODE:-fast}
      # 1, not 2: Ollama runs on the same host and serializes on one GPU.
      # Measured on an M1 — 3.87s/call at 1, 4.93s at 2, 20.62s at 4.
      VLM_CONCURRENCY: ${VLM_CONCURRENCY:-1}
      # qwen3.5 is a reasoning model and Ollama defaults thinking ON. Measured
      # on a real BERT table crop: 87.35s versus 2.27s, with the reasoning
      # discarded unread. Tables additionally returned empty every time.
      OLLAMA_VLM_THINK: ${OLLAMA_VLM_THINK:-false}
      # Tables go to docling's TableFormer. A 0.8B VLM cannot read them: on
      # bert.pdf the 13-column GLUE table came back with headers "I, II, III…"
      # and a small table came back with invented rows. Set true to restore the
      # old behaviour of routing complex tables to the VLM.
      VLM_TABLES: ${VLM_TABLES:-false}
      # Latency here is pure decode at ~35 tok/s, so elapsed IS the output
      # length — and Ollama's defaults (temperature 0.8, num_predict -1)
      # leave it unbounded. Measured: the same 218x54px crop returned 22
      # tokens in 1.55s and 342 tokens of invented flowchart in 10.94s on
      # consecutive identical requests, and the worst call of a 191-call
      # run took 93.3s. Greedy decoding gives 22-35 tokens and transcribes
      # the crop instead of inventing one.
      OLLAMA_VLM_TEMPERATURE: ${OLLAMA_VLM_TEMPERATURE:-0.0}
      OLLAMA_VLM_NUM_PREDICT: ${OLLAMA_VLM_NUM_PREDICT:-384}
      # Short-side floor for sending a picture to the VLM. 113 of those 191
      # calls were strips under 64px tall — rules and equation lines, not
      # figures — and they consumed 60% of the VLM budget hallucinating
      # content that then gets embedded.
      VLM_IMAGES_SCALE: ${VLM_IMAGES_SCALE:-2.0}
      VLM_MIN_IMAGE_SHORT_PT: ${VLM_MIN_IMAGE_SHORT_PT:-107}

      # The runtime image ships without a compiler by design, so a downstream
      # torch.compile path (torchvision NMS during docling's layout pass) dies
      # with InvalidCxxCompiler. Nothing here calls torch.compile, so forcing
      # eager execution costs nothing and removes the crash.
      TORCHDYNAMO_DISABLE: "1"
    volumes:
      - .:/app
      - ./logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - rag_network
    restart: unless-stopped

  # ============================================
  # Langfuse (Optional - LLM Observability UI)
  # Start with: docker compose --profile observability up langfuse
  # UI at: http://localhost:3000
  # ============================================
  langfuse:
    image: langfuse/langfuse:2.80
    container_name: rag_langfuse
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          memory: 256M
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:?}:${POSTGRES_PASSWORD:?}@postgres:5432/langfuse_db
      NEXTAUTH_SECRET: ${LANGFUSE_NEXTAUTH_SECRET:-changeme-use-a-real-secret-in-prod}
      SALT: ${LANGFUSE_SALT:-changeme-use-a-real-salt-in-prod}
      NEXTAUTH_URL: http://localhost:3000
      LANGFUSE_INIT_ORG_ID: ${LANGFUSE_INIT_ORG_ID:-rag-org}
      LANGFUSE_INIT_PROJECT_ID: ${LANGFUSE_INIT_PROJECT_ID:-rag-project}
      LANGFUSE_INIT_PROJECT_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-lf-pk-placeholder}
      LANGFUSE_INIT_PROJECT_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-lf-sk-placeholder}
      LANGFUSE_INIT_USER_EMAIL: ${LANGFUSE_INIT_USER_EMAIL}
      LANGFUSE_INIT_USER_PASSWORD: ${LANGFUSE_INIT_USER_PASSWORD}
      LANGFUSE_INIT_USER_NAME: ${LANGFUSE_INIT_USER_NAME}
      REDIS_HOST: redis
      REDIS_PORT: 6379
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - rag_network
    restart: unless-stopped

  # ============================================
  # pgAdmin (Optional - Database Management UI)
  # ============================================
  pgadmin:
    image: dpage/pgadmin4:8.14
    container_name: rag_pgadmin
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:?PGADMIN_EMAIL is required}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:?PGADMIN_PASSWORD is required}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    networks:
      - rag_network
    restart: unless-stopped
    profiles:
      - dev  # Only start with: docker-compose --profile dev up

  # ============================================
  # Test Runner Service
  # ============================================
  test:
    build:
      context: .
      dockerfile: deploy/deployment/Dockerfile.test
    image: rag_with_llama:test
    container_name: rag_test
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          memory: 128M
    environment:
      # Database connection (use service name 'postgres')
      DB_HOST: postgres
      DB_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-rag_db}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}

      # API Keys
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      LOGFIRE_WRITE_TOKEN: ${LOGFIRE_WRITE_TOKEN:-}
      LOGFIRE_READ_TOKEN: ${LOGFIRE_READ_TOKEN:-}

      # LLM Configuration
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}

      # Test environment
      PYTHONPATH: /app
    volumes:
      # Mount tests directory for easier test development
      - ./tests:/app/tests
      # Mount htmlcov for coverage reports
      - ./htmlcov:/app/htmlcov
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rag_network
    profiles:
      - test  # Only start with: docker-compose --profile test up

# ============================================
# Networks
# ============================================
networks:
  rag_network:
    driver: bridge

# ============================================
# Volumes (Persistent Data)
# ============================================
volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local
  # docling's layout model (heron, 171MB) and TableFormer (212MB), shared by the
  # two workers. These are downloaded on first parse, not baked into the image
  # the way the MiniLM embedding model is (deploy/deployment/Dockerfile:81-84).
  # Without a volume they sit in the container's writable layer, so any recreate
  # — including the --force-recreate that the F18 verification procedure calls
  # for — discards them and the next parse re-downloads ~385MB before page 1.
  hf_cache:
    driver: local
